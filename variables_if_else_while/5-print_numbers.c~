#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: print 0 to 9
 * parameter: void
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
for (int i = 0; i <=9 ; i++)
{
printf(i);
}
putchar('\n');
return (0);
}
