#include "main.h"

/**
 * puts2 - Entry point
 *
 * Description: prints
 * @str: chaine de character
 *
 * Return: Always 0 (Success)
 */

void puts2(char *str)
{
char c;
while (*str != '\0')
{
c = *str;
_putchar(c);
*str++;
*str++;
}
_putchar('\n');
}
